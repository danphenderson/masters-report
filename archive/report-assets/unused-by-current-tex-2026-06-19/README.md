# Report Assets Unused by Current TeX

This archive contains report assets moved out of the active
`figures/static/static/` tree on 2026-06-19.

The archived files were not reached by the current `final-report.tex`
dependency graph, direct `\includegraphics` calls, active `\figtikz` calls, or
the inspected appendix provenance paths at cleanup time. They are preserved
instead of deleted because some files were generated by historical renderer
workflows or may be useful for older report layouts.

Future figure regeneration workflows may need to restore files from this
archive before running older commands. Restore only the needed files, rerun the
TeX/reference checks, and keep any refreshed report artifacts in a separate
patch from manuscript source edits.
