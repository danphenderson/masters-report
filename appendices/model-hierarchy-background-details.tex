\section{Model-Hierarchy Background Pointers}
\label{app:model-hierarchy-background-details}

The detailed 0D, 1D, 2D, 3D/FSI, and coupled-model notes now appear directly
in Sec.~\ref{sec:bloodmodels}. This appendix is retained as a stable pointer
for older references and source-map entries. It no longer duplicates the model
hierarchy chapter.

For quick navigation, use:
\begin{itemize}
    \item Sec.~\ref{subsec:0dmodels} for 0D outlet and network models;
    \item Sec.~\ref{subsec:1d-area-flow-details} for the 1D area-flow model,
    wall law, stenosis geometry, boundary data, and severity outputs;
    \item the 2D intermediate-tier subsection in Sec.~\ref{sec:bloodmodels}
    for simplified resolved-flow comparison roles;
    \item Sec.~\ref{subsec:3dmodels} for 3D and FSI comparison roles;
    \item Sec.~\ref{subsec:coupledmodels} for coupled 3D--1D--0D workflows.
\end{itemize}
