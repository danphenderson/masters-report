\section{Mathematical Model Hierarchy}
\label{sec:model-hierarchy}

The central organizing device of the review is the model hierarchy. The
hierarchy is not a ladder of accuracy. It is a way to ask what a formulation
retains, what it closes, what it can observe natively, and what it must map
before a comparison is meaningful. A resolved three-dimensional model retains
local velocity and pressure fields in the lumen. A fluid--structure interaction
model additionally retains wall motion and interface work. Axisymmetric and
two-dimensional models retain selected local structure under stronger geometry
assumptions. Distributed one-dimensional models retain axial propagation of
area and flow. Zero-dimensional and network models retain compartmental
pressure--flow relations. Multiscale models couple several of these states.
Surrogate, PINN, and operator-learning models approximate solution or
parameter-to-solution maps and inherit the model tier, data, and loss
conventions used to train or constrain them
\cite{QuarteroniFormaggia2004CardiovascularModeling,FormaggiaEtAl2003OneDimensionalBloodFlow,ShiEtAl2011BloodFlowModelReview,KopplHelmig2023DimensionReducedModeling}.

\subsection{Resolved 3D CFD and FSI Models}
\label{subsec:resolved-reduced-model-tiers}

The resolved continuum tier starts from conservation of mass and momentum on a
three-dimensional lumen. In a fixed-wall CFD model the wall is a prescribed
geometric boundary, while in resolved FSI the wall has structural degrees of
freedom and transfers kinematic and traction information through a moving
interface
\cite{QuarteroniFormaggia2004CardiovascularModeling,FormaggiaEtAl2007FSICoupling}.
These models can expose local velocity gradients, wall traction, pressure
fields, recirculation, separation, and geometry-specific flow structures. A WSS
quantity, for example, is not just a scalar label: it requires a stress tensor,
wall normal, tangent projection convention, and surface output map
\cite{JohnEtAl2017WallShearStressVectorForm}.

The resolved tier also contains distinct uses of ``three-dimensional'' data.
Classical numerical analysis of blood flow in the heart already treated the
geometry and moving-boundary representation as part of the mathematical problem
\cite{Peskin1977NumericalAnalysisBloodFlowHeart}. Continuum cardiovascular
models and 3D--1D FSI coupling sources then make the interface and wall state
explicit
\cite{QuarteroniFormaggia2004CardiovascularModeling,FormaggiaEtAl2007FSICoupling}.
By contrast, a fixed-wall resolved stenosis computation used to estimate a
pressure or velocity diagnostic may retain the same local velocity and pressure
fields while closing wall motion and downstream circulation differently. The
source-to-source contrast is therefore not simply 3D versus reduced. It is
which local fields are retained, which wall and boundary variables are closed,
and which observable is extracted from the resulting field.

\subsection{Axisymmetric and Two-Dimensional Reductions}
\label{subsec:axisymmetric-two-dimensional-models}

Axisymmetric and two-dimensional models occupy an intermediate role. They
retain more geometry and local velocity information than a one-dimensional
area--flow model, but they remove at least one spatial direction or impose
symmetry. This makes them useful for idealized stenosis studies, sectional
diagnostics, and controlled comparisons, while still requiring declared
boundary conditions, rheology, wall treatment, and observable definitions. The
interpretive limit is the same as in any reduced model: suppressed secondary
flow, asymmetric geometry, and wall mechanics cannot later be recovered as
native outputs unless an additional closure or observation map supplies them.

\subsection{Distributed One-Dimensional Area--Flow Models}
\label{subsec:distributed-one-dimensional-models}

Distributed 1D models reduce each vessel segment to axial variables, most often
cross-sectional area, flow, mean velocity, and pressure. They preserve pulse
propagation and vessel-to-vessel transport at far lower cost than resolved
CFD/FSI, but profile, wall, friction, rheology, stenosis, and boundary effects
enter as closures rather than resolved fields
\cite{FormaggiaEtAl2003OneDimensionalBloodFlow,KopplHelmig2023DimensionReducedModeling}.
This tier is the natural setting for single-vessel stenosis calculations whose
native outputs are area, flow, mean velocity, and reduced pressure.

Stenosis-specific reductions also divide by observable. Pressure-loss formulas
and pressure-drop reduced-order models target obstruction through a flow--loss
relation, while distributed 1D stenosis models place narrowed reference
geometry and wall response inside an area--flow system. Network-scale coronary
models add tree structure and terminal assumptions, so their pressure and flow
outputs must be read with the same closure discipline
\cite{LyrasLee2021PressureDropROM,DuanmuEtAl2019CoronaryArterialTree,CanicEtAl2024Extended1DStenosis}.

\subsection{Zero-Dimensional Networks and Multiscale Coupling}
\label{subsec:network-multiscale-learned-models}

Zero-dimensional and network models replace distributed fields with lumped
states and algebraic or ordinary-differential pressure--flow relations. They
are central for terminal resistance, compliance, circulation closure, and large
arterial networks, but their state variables are not local wall or velocity
fields \cite{ShiEtAl2011BloodFlowModelReview}. In a single-vessel calculation,
these models most often enter through inlet or outlet closure rather than as
the main spatial solver.

Geometrical multiscale models connect tiers, for example 3D--1D, 3D--0D, or
1D--0D, through interface conditions that transfer pressure, flow, traction,
impedance, or characteristic information
\cite{QuarteroniVeneziani2003GeometricalMultiscale,FormaggiaEtAl2007FSICoupling}.
The mathematical issue is not only that two solvers are coupled; it is that the
interface variables must have compatible meanings across dimensions. Coronary
FFR comparisons between 1D and 3D models show the same issue on the output
side: the reduced and resolved states must be mapped to a common pressure-ratio
observable before agreement or disagreement has a mathematical meaning
\cite{Blanco2018FFR1D3D}.

\subsection{Learned Surrogates, PINNs, and Operator Maps}
\label{subsec:learned-surrogates-operator-maps}

Surrogate and scientific-ML models add a second axis to the hierarchy. A PINN
uses residual, boundary, data, and regularization terms to constrain an
approximate solution or inverse map
\cite{RaissiEtAl2019PhysicsInformedNeuralNetworks,YuEtAl2026PINNHemodynamicsReview}.
Operator-learning models, such as DeepONet and Fourier neural operators, learn
maps between functions or parameterized fields
\cite{LuEtAl2021DeepONet,LiEtAl2021FNO}. Hemodynamics-specific neural
differential and deep-operator examples target reduced blood-flow or
stenosed-vessel maps directly
\cite{CsalaEtAl2025PCNDE,VelikorodnyEtAl2025DeepOperatorStenosed}. These
learned categories do not remove the need to state the underlying model tier: a
neural surrogate of a 1D model has different native outputs from a neural
surrogate trained on resolved 3D velocity fields, and its validation observable
must match the map it was trained to approximate.

\subsection{Native States and Cross-Tier Observation Maps}
\label{subsec:native-states-observation-maps}

Table~\ref{tab:model-hierarchy-comparison} summarizes the hierarchy by native
outputs, main closures, and typical role. The table is intentionally organized
around state and observation rather than around individual papers. It is the
reading frame used by later closure, evidence, and synthesis chapters.

\begin{table}[!htb]
    \centering
    \scriptsize
    \caption{Compact comparison of cardiovascular model tiers. ``Native
    outputs'' means quantities retained directly by the mathematical state,
    before an observation or reconstruction map is applied.}
    \label{tab:model-hierarchy-comparison}
    \begin{tabularx}{\textwidth}{@{}
        >{\raggedright\arraybackslash}p{0.19\textwidth}
        >{\raggedright\arraybackslash}p{0.27\textwidth}
        >{\raggedright\arraybackslash}p{0.25\textwidth}
        >{\raggedright\arraybackslash}X@{}}
        \toprule
        \textbf{Category}
            & \textbf{Native outputs}
            & \textbf{Main closures}
            & \textbf{Typical role} \\
        \midrule
        3D CFD
            & Lumen velocity, pressure, stress-derived wall quantities.
            & Geometry, rheology, wall boundary, inlet/outlet data, numerical
            resolution.
            & Local hemodynamics, pressure and velocity fields, WSS diagnostics. \\
        Resolved FSI
            & Fluid fields plus wall displacement, wall velocity, structural stress.
            & Structural model, material law, interface coupling, fluid closures.
            & Moving-wall mechanics and fluid--structure interaction. \\
        Axisymmetric / 2D
            & Reduced spatial velocity and pressure fields on a symmetry or
            sectional domain.
            & Symmetry assumptions, reduced geometry, boundary data, wall treatment.
            & Controlled stenosis studies and lower-cost resolved diagnostics. \\
        Distributed 1D
            & Axial area, flow, mean velocity, pressure or pressure surrogate.
            & Profile, wall law, friction, rheology, stenosis geometry, boundary
            rules.
            & Network-ready pulse propagation and single-vessel stenosis studies. \\
        0D / network
            & Lumped pressures, volumes, flows, and terminal states.
            & Resistance, compliance, inertance, valves, junctions, terminal beds.
            & Circulation closure, outlet models, and large-system screening. \\
        Geometrical multiscale
            & Coupled tier-specific states with shared interface variables.
            & Interface pressure/flow/traction/impedance laws and stability rules.
            & Coupling local resolved regions to reduced or lumped circulation. \\
        Surrogate / PINN / operator learning
            & Learned solution fields, parameters, or function-to-function maps.
            & Training data, residual loss, architecture, normalization, model tier.
            & Fast emulation, inverse estimation, and parametric maps. \\
        \bottomrule
    \end{tabularx}
\end{table}

\begin{stenosisillustration}
\label{ex:stenosisillustration-model-hierarchy}
The case study later in the report sits in the distributed 1D branch. Its
mathematical state is a single-vessel area--flow system with an analytic
stenosis reference geometry and reduced wall/friction/profile closures. The
case therefore belongs beside other 1D stenosis models, including extended
variable-radius formulations \cite{CanicEtAl2024Extended1DStenosis}, rather
than beside resolved CFD or FSI as a native wall-traction or wall-displacement
solver. When it is compared with resolved data, the comparison passes through
an observation map from 1D area--flow variables to the chosen velocity, flow, or
pressure quantity.
\end{stenosisillustration}
